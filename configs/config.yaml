# Hydra parameters, can be passed via commandline when running main.py

image_size: 64 
patch_size: 4

# Model 
# cnn, vit or hybrid 
model: vit 
pretrain: False 

# Training loop parameters 
epochs: 10
lr: .0001

# Training 
batch_size: 16
cuda: True 
device: cpu
