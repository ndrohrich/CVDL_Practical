# Hydra parameters, can be passed via commandline when running main.py

# Model 
# cnn, vit or hybrid 
model: vit 
pretrain: False 

# Training loop parameters 
epochs: 10
lr: .0001

# Training 
batch_size: 16
cuda: True 
device: cpu
